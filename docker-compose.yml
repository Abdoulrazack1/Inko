# ============================================================
# Inko — orchestration de production (app + MySQL + Redis)
# ============================================================
# Démarrage :  docker compose up -d --build
# Accès      :  http://localhost:8088
# Personnalise les mots de passe via un fichier .env à côté de ce fichier
# (DB_PASSWORD=...) ou en éditant les valeurs ci-dessous.

services:
  inko-db:
    image: mysql:8
    container_name: inko-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-inko_root}
      MYSQL_DATABASE: ${DB_NAME:-inko}
    volumes:
      - inko-db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-p${DB_PASSWORD:-inko_root}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks: [inko]

  inko-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inko-app
    restart: unless-stopped
    depends_on:
      inko-db:
        condition: service_healthy
    environment:
      PORT: 8080
      DB_HOST: inko-db
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: ${DB_PASSWORD:-inko_root}
      DB_NAME: ${DB_NAME:-inko}
      JWT_SECRET: ${JWT_SECRET:-change-me-in-prod}
      # Liste blanche CORS en prod (laisser vide = permissif). Ex :
      # CORS_ORIGINS: "https://inko.exemple.com"
      TRUST_PROXY: 1
      REDIS_URL: redis://inko-cache:6379
    ports:
      - "8088:8080"
    networks: [inko]

  # Réservé pour un cache partagé (couvertures, sessions) — l'app peut
  # l'adopter plus tard via REDIS_URL ; non bloquant s'il est absent.
  inko-cache:
    image: redis:7-alpine
    container_name: inko-cache
    restart: unless-stopped
    networks: [inko]

volumes:
  inko-db-data:

networks:
  inko:
    driver: bridge
