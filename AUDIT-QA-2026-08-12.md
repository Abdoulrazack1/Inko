# Audit QA Inko — campagne du 12 août 2026

Version auditée : **2.5.7**. Complément à `AUDIT-INKO-2026-07-28.md`, dont il
reprend le format et la sévérité (🔴 bloquant · 🟠 sérieux · 🟡 à traiter ·
⚪ confort).

---

## Méthode

Ce qui a réellement été exécuté, pas ce qui était prévu :

| Campagne | Portée |
|---|---|
| Balayage de pages | 20 pages : erreurs console, requêtes en échec, poids, a11y statique |
| **Actionnement des contrôles** | **20 pages**, **1 371 contrôles distincts** inventoriés (3 213 occurrences), **786 actionnés** un par un, page rechargée avant chaque action |
| **Base de données** | 23 tables inspectées : moteurs, encodages, clés étrangères, orphelins, cohérence, croissance |
| **Surface d'API** | **67 routes GET** appelées une par une, sans jeton puis authentifié |
| Sécurité API | 7 charges SSRF, 7 routes privées sans jeton, en-têtes, CORS, limitation de débit, forme des erreurs, charge utile démesurée |
| Mobile | 10 pages à 375 × 812 |
| Divers | 4 thèmes, bascule i18n, hors-ligne, 12 arrêts de tabulation, poids des assets, santé des 9 sources, fan-out réseau de 4 pages |
| **Passage approfondi** | Lecteur de romans (20 contrôles + 13 réglages + contraste des 4 thèmes + persistance), barre de recherche (anti-rebond, suggestions, caractères spéciaux), notifications (4 filtres) |
| **Application installée** | App desktop **2.5.7 lancée**, ses 24 pages balayées sur son propre serveur : violations CSP, erreurs, rendu — environnement où la CSP est active et le CORS fermé |

**Contrôle distinct** = (balise + type + libellé). La bibliothèque expose 1 170
éléments dont ~700 cœurs de cartes identiques : les cliquer un par un aurait
mesuré la même chose 700 fois. Le rapport note le nombre d'occurrences.

**Non destructif.** Les actions irréversibles sur les vraies données —
supprimer, vider, réinitialiser, déconnecter, importer/exporter, restaurer —
ont été **recensées, jamais déclenchées**. Elles restent donc **non auditées**
et devront l'être sur une base jetable.

---

# PARTIE I — Corrections de mes propres erreurs

À faire figurer en premier, parce que plusieurs constats que j'ai annoncés à
l'oral étaient faux, et qu'un rapport qui masque ses erreurs de mesure ne vaut
rien. Voir aussi ERR-05 en partie VI.

### ERR-01 — « 53 contrôles morts » : **zéro** après vérification

Mon détecteur classait un contrôle « inerte » si la taille du DOM variait de
moins de 30 caractères après le clic. Ce critère est aveugle à tout ce qui ne
change qu'un style, une classe ou un état interne. Vérification un par un des
53 suspects, avec l'observable adapté :

| Contrôle suspecté | Verdict après vérification |
|---|---|
| 8 pastilles de couleur d'accentuation | **fonctionnent** — `--accent` passe de `#c1531b` à `#3b82f6` |
| Onglets bibliothèque (Mises à jour, Signets, Téléchargements) | **fonctionnent** — onglet actif et contenu changent |
| Vue liste / grille / densité (bibliothèque) | **fonctionnent** — `lib2-grid` → `lib2-list`, 3 → 1 colonne, `--compact` |
| Grille / Liste (catalogue) | **fonctionne** — `results-grid` → `results-grid list-view`, 5 → 1 colonne |
| 4 boutons « Modifier le raccourci » | **fonctionnent** — le bouton passe à `…` et attend une frappe physique |
| Liens de pied de page (Code source, Bug, Versions, Licence) | **fonctionnent** — `href` GitHub + `target="_blank"` |
| « Aléatoire » | **fonctionne** — navigue vers `serie.html?id=…` |

**Aucun contrôle mort n'a été trouvé sur les 786 actionnés.**

### ERR-02 — « Les boutons FR / EN ne changent pas la langue » : faux

Ce ne sont pas des boutons de langue d'interface, mais le filtre de **langue de
traduction** (`parametres.html:145-147`, `data-val="fr,en" | "fr" | "en"`).
Je vérifiais `document.documentElement.lang`, qui n'a aucune raison de bouger.
De plus, `en` était **déjà actif** au moment du clic.

### ERR-03 — « L'internationalisation ne couvre que 3 clés » : faux

`assets/i18n/en.json` contient **861 chaînes + 55 motifs**. J'avais compté les
clés racine (`keys`, `strings`, `patterns`) au lieu de leur contenu.

### ERR-04 — « CSP absente, CORS permissif » : ce ne sont pas des défauts

`security.js:26` : `CSP_ON = (IS_PROD || IS_DESKTOP) && DISABLE_CSP !== '1'`.
Les deux sont **délibérément** inactifs hors production et hors desktop
(audits S-6 et S-1). Mon sondage tournait sur le serveur de développement.
Ce qui reste vrai, c'est la remarque OPS-01 ci-dessous.

---

# PARTIE II — Constats

## II.1 Sécurité

### SEC-01 🔴 `POST /api/auth/local` délivre un jeton **admin** à qui atteint le port

Le serveur écoute sur `::` (toutes interfaces, `app.js:161` — `app.listen(PORT)`
sans hôte). Depuis l'**adresse réseau** de la machine, pas la boucle locale :

```
$ curl -X POST http://192.168.1.34:8088/api/auth/local -d '{}'
{"user":{"id":26,"username":"Kaito","email":"demo@inko.app","role":"admin",…},
 "token":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9…","localMode":true}
```

`routes/index.js:74` :
```js
router.post('/auth/local',          Auth.localAuth);   // mode local sans écran de connexion
```

Aucun intergiciel : ni authentification, ni filtre d'adresse, ni limitation de
débit — alors que `/auth/login` et `/auth/register` portent `authLimiter`.
`auth.controller.js:100-113` (`localAuth`) résout le compte propriétaire, le
**promeut `admin`** s'il ne l'est pas, signe un jeton et pose le cookie.

Le mode « façon Mihon, sans écran de connexion » est un choix produit
légitime ; ce qui manque est le garde qui le rend sûr. Le mode hub (AMEL-93)
sert explicitement les autres appareils du foyer : **toute personne sur le même
Wi-Fi devient administrateur de la bibliothèque**.

**Correctif** : restreindre à la boucle locale (`req.ip` ∈ `127.0.0.1`, `::1`)
et exiger un appairage explicite au-delà.

### SEC-02 🔴 Toutes les installations desktop partagent le même secret JWT

`lib/secret.js:35` :
```js
module.exports = SECRET && !isWeak(SECRET) ? SECRET : 'inko-dev-secret-change-me';
```

En production, un secret faible fait sortir le processus. En desktop, non : le
sidecar tourne sans `NODE_ENV=production` et retombe sur la **constante
littérale**. Les jetons sont donc forgeables hors ligne, sans toucher au
serveur. Combiné à SEC-01, la porte est ouverte deux fois.

**Correctif** : générer un secret par installation au premier démarrage. Le
modèle existe — `db-credentials.json` fait exactement cela pour la base.

### SEC-03 🟠 53 des 61 routes d'écriture n'ont aucune limitation de débit

Relevé sur `routes/index.js` (128 routes, 61 en écriture) :

```
POST   /auth/local           [SANS AUTH, SANS LIMITE]
POST   /auth/google          [SANS AUTH, SANS LIMITE]
POST   /auth/logout          [SANS AUTH, SANS LIMITE]
PUT    /auth/password        · POST /auth/delete · PUT /auth/profile
DELETE /auth/sessions/:id    · DELETE /auth/sessions/others
POST   /extensions/update    · POST /extensions/:id/uninstall · …/reinstall
POST   /me/favorites         · DELETE /me/favorites/:mangaId · …
```

L'audit de juillet relevait SEC-10 « 18 routes de relais sans rate-limit » ; la
surface réelle est trois fois plus large.

### SEC-04 🟠 Sauvegardes en clair par défaut

`lib/backup.js:27` : `PASSPHRASE = process.env.BACKUP_PASSPHRASE || ''`. Le
dump quotidien contient l'email et la bibliothèque de **tous** les comptes. Le
chiffrement AES-256-GCM est écrit, il n'est pas activé. Même correctif que
SEC-02 : passphrase générée à l'installation.

### ✅ Ce qui a résisté

| Sonde | Résultat |
|---|---|
| SSRF sur `/api/img` : `127.0.0.1:8088`, `169.254.169.254`, `[::1]`, `10.0.0.1`, `192.168.1.1`, `localhost:3306`, `file:///C:/Windows/win.ini` | **7/7 bloquées** (403 ; 400 pour `file://`) |
| `/api/me/*`, `/api/admin/*` sans jeton | **401** sur les 6 testées |
| En-têtes | `X-Content-Type-Options: nosniff` · `frame-ancestors` · `Referrer-Policy: no-referrer` · `X-Powered-By` masqué |
| `authLimiter` sur `/auth/login` | **429 au 13ᵉ essai** |
| Forme des erreurs | `{"error":"Endpoint introuvable"}` — aucune pile |

---

## II.2 Bugs fonctionnels

### BUG-01 🔴 Des identifiants sont envoyés à la mauvaise source — et le serveur répond `200`

Mesuré sur 3 pages, en observant chaque requête `/api/sources/<src>/mangas/<id>`
et en comparant la **forme** de l'identifiant à celle attendue par la source
(ULID = WeebCentral, UUID = MangaDex, numérique = Gutenberg, slug = SushiScan…) :

```
14 requêtes avec un identifiant étranger à la source appelée
   weebcentral ← identifiant numérique (Gutenberg)  ×2   → 200
   weebcentral ← slug (SushiScan / Gutenberg-fr)    ×8   → 200
   weebcentral ← slug                                ×4   → 504
exemples : /api/sources/weebcentral/mangas/2701
           /api/sources/weebcentral/mangas/le-siege-des-exilees
           /api/sources/weebcentral/mangas/get-backers
```

`2701` est l'identifiant de *Moby Dick* chez Project Gutenberg.

**Cause.** `api.js:496` :
```js
getFrom: (source, id) => get((source ? `/sources/${encodeURIComponent(source)}` : '') + `/mangas/${encodeURIComponent(id)}`)
```
Quand `source` est absent, l'URL part **sans préfixe de source** et le serveur
applique la source courante. Or `profil.js:581` appelle
`API.mangas.getFrom(i.source, i.mangaId)` sur des entrées d'historique, et
`profil.js:1152-1160` documente précisément que **la table `events` ne porte pas
de source** :

```js
// La table `events` ne porte PAS de source : `item.source` etait donc
// toujours indefini et readerHref retombait sur la source COURANTE. Un
// roman Gutenberg consulte depuis une session ouverte sur weebcentral
// ouvrait ainsi le lecteur d'IMAGES — page blanche garantie.
```

Le correctif `sourceDe()` a été appliqué à `readerHref`, **pas** à `getFrom`.

**Deux conséquences.** Le `200` est pire que l'erreur : voir BUG-02. Et la
description « page blanche garantie » correspond au signalement *« les pages des
chapitres ne se chargent pas »* — c'est la piste la plus sérieuse.

### BUG-02 🟠 Un identifiant inconnu renvoie `200` avec une fiche vide au lieu de `404`

```
GET /api/sources/mangadex/mangas/%00%01
→ 200 {"title":"","titleAlt":"","author":"","description":"","tags":[],"cover":null,…}
```

L'app affiche donc une fiche vide plutôt qu'un « série introuvable ». C'est ce
qui rend BUG-01 silencieux.

### BUG-03 🟠 L'app déclenche sa propre limitation de débit

`429 /api/me/updates` observé **7 fois, sur 5 pages différentes**
(`parametres`, `profil`, `bibliotheque`, `chapitre`, `lecture`) au cours d'une
navigation normale — ainsi que `429` sur `/api/sources/novelbin/mangas/search`,
`/api/sources/gutenberg-fr/mangas/search`, `/api/sources/mangadex/mangas/:id`.

Le limiteur ne distingue pas l'app elle-même d'un client tiers.

### BUG-04 🟠 `profil.html` : 26 scrapes externes par affichage

```
Appels /api total ............................ 53
dont /api/sources/weebcentral/mangas/:id ..... 26
```
L'audit de juillet mesurait 201 (BUG-02 d'alors). Ramené à 26, mais le motif
demeure : une requête par série vers une source scrapée, d'où les `502`/`504`
en rafale relevés à l'actionnement des contrôles.

### BUG-05 🟠 Reprendre un roman appelle le lecteur d'images

```
501 /api/sources/gutenberg/chapters/2701%3Afull/pages   ← via « Resume my last read »
```
`pages` est l'endpoint des planches ; Gutenberg est une source de texte et
renvoie donc « non implémenté ». Même famille que BUG-01.

### BUG-06 🟠 Trois sources hors service, l'alarme sonne dans le vide

| Source | Symptôme |
|---|---|
| `novelbin` | 0 résultat — `check-sources` : « balisage ou URL du site probablement changés » |
| `gutenberg-fr` | `popular` → 0 titre (la recherche répond) |
| `chireads` | 3 titres, **0 chapitre** sur le premier |

Le workflow `sources-health` est **rouge depuis le 20 juillet, 4 lundis
consécutifs**. Il fonctionne, il dit vrai, personne n'agit.

### BUG-07 🟠 `weebcentral` ignore `limit`

```
GET /api/sources/weebcentral/mangas/popular?limit=3   → 32 résultats
```
Toute pagination bâtie sur cette source est fausse. Aucun test de contrat
d'extension n'existe pour l'attraper.

### BUG-08 🟡 Lecteur page par page : `data-idx` figé

Sur un chapitre de 32 pages, la première flèche droite reste sans effet ; à la
seconde l'image change mais `data-idx` reste à `0`.

### BUG-09 ⚪ `gen-precache` salit `service-worker.js` à chaque exécution

Sous Windows, le générateur réécrit en LF un fichier livré en CRLF :
`git status` n'est plus propre après un `npm run gen-precache` sans changement.

### BUG-10 ⚪ `CACHE_VERSION` ne dépend pas du contenu

Empreinte `ce7c71` identique de la 2.5.0 à la 2.5.7 malgré des assets modifiés :
seul le numéro de version fait varier la clé de cache.

---

## II.3 Mobile — le domaine le plus dégradé

### MOB-01 🔴 Débordement horizontal sur 6 pages sur 10 à 375 px

```
sources.html ......... +488 px      profil.html ......... +145 px
parametres.html ...... +111 px      notifications.html ... +80 px
accueil.html .......... +78 px      bibliotheque.html .... +52 px
```
Un débordement de 488 px sur un écran de 375 ne se règle pas au zoom.

### MOB-02 🟠 Cibles tactiles sous 32 px

`bibliotheque` **770** · `catalogue` 132 · `accueil` 61 · les 7 autres pages
entre 19 et 39. C'est AMEL-84, jamais traité.

---

## II.4 Performance

### PERF-01 🟠 `notifications.html` transfère 1,9 Mo

~28 couvertures **pleine taille** (42 à 155 Ko chacune) pour une liste de
notifications. Les champs `coverThumb` existent et ne sont pas utilisés.

### PERF-02 🟡 `bibliotheque.html` : 109 requêtes au chargement, dont 90 `/api/img`

Contre 22 à 37 pour les pages calmes.

### PERF-03 🟡 5 vendors sur 6 hors précache

| Fichier | Poids | Précaché |
|---|---|---|
| `pdf.worker.min.js` | 1 062 Ko | non |
| `three.min.js` | 594 Ko | non |
| `pdf.min.js` | 313 Ko | non |
| `gsap.min.js` | 71 Ko | non |
| `ScrollTrigger.min.js` | 43 Ko | non |
| `jszip.min.js` | 95 Ko | oui |

S'y ajoutent `motion.js` et `music.js`, chargés dynamiquement par `global.js`
et absents eux aussi. Hors ligne, la 3D, les animations, la musique et le
lecteur PDF échouent en silence — alors que l'app promet un mode hors ligne.

### ✅ Solide
gzip actif · `immutable` 30 jours sur les vendors · `must-revalidate` sur le
code applicatif (cohérent avec le service worker network-first) · pages entre
153 et 358 Ko hors notifications.

---

## II.5 Accessibilité

| # | Constat | Détail |
|---|---|---|
| A11Y-01 🟡 | 4 pages sans `<h1>` | `liste`, `localreader`, `recherche`, `u` |
| A11Y-02 🟡 | 3 pages sans `<main>` | `anilist`, `localreader`, `offline` |
| A11Y-03 🟡 | 8 champs sans étiquette accessible | biblio 2 · profil 2 · stats 2 · import 1 · localreader 1 · parametres 1 · recherche 1 |

**✅ Solide** : 0 image sans `alt` sur 20 pages · 0 bouton sans nom accessible ·
indicateur de focus présent sur les 12 premiers arrêts de tabulation · ordre de
tabulation cohérent · les 4 thèmes déclarés s'appliquent · axe-core vert en
critical/serious · thème contraste AAA.

---

## II.6 Code mort et cohérence

### DEAD-01 🟡 `assets/css/auth-unified.css` — 1 069 lignes, zéro référence

Aucune page, aucun script, aucune injection dynamique dans tout le dépôt.

---

## II.7 Exploitation

### OPS-01 🟠 Rien n'annonce la posture de sécurité réelle

Sans `NODE_ENV=production` : pas de CSP, CORS qui reflète n'importe quelle
origine (vérifié : `https://site-tiers.test` renvoyé tel quel), secret de
développement. C'est cohérent et documenté, mais un auto-hébergeur qui lance
`node server/server.js` sur un VPS obtient tout cela **sans qu'aucun message ne
l'avertisse**.

**Correctif** : bannière au démarrage énonçant la posture dès que le serveur
écoute ailleurs que sur la boucle locale.

---

---

# PARTIE IV — Base de données

*Inspection directe de MySQL (23 tables, 26 comptes, 392 favoris), le
12 août 2026.*

### DB-01 🔴 `sessions` grossit d'une ligne par **page affichée**, et rien ne l'expire

```
lignes ................. 1 820   (1,6 Mo — la plus grosse table de la base)
utilisateurs concernes ....... 1   (user_id 26 : les 1 820 lignes)
fenetre .................... 82 h
pic ..................... 626 lignes en une heure
plus ancienne ...... 2026-08-09 04:58
```

Structure réelle :
```
id char(36) · user_id int · user_agent varchar(255) · ip varchar(45)
created_at timestamp · last_seen_at timestamp
```

**Aucune colonne d'expiration ni de révocation.** La migration 16 s'intitule
pourtant « sessions révocables une à une (audit AMEL-69) » : la révocation ne
peut donc se faire que par suppression de ligne, et **rien ne purge**.

La cause de la croissance : `/api/auth/local` est appelé **à chaque chargement
de page** (vérifié : 1 appel sur `accueil`, `profil`, `bibliotheque`, `stats`),
et `localAuth` signe un jeton, donc insère une session. Une session par page
vue, conservée indéfiniment.

Les 626 lignes de 14 h correspondent à ma propre campagne d'audit — ce qui est
exactement la démonstration : **le volume suit le nombre de pages affichées, pas
le nombre de connexions**.

**Correctifs** : ajouter `expires_at`, purger au démarrage et périodiquement,
et surtout réutiliser la session existante dans `localAuth` au lieu d'en créer
une à chaque appel.

### DB-02 🟠 `progress.source` est NULL sur 15 lignes — c'est la racine de BUG-01

```
SELECT manga_id, source FROM progress WHERE source IS NULL OR source='';

01J76XYC2K8QWFZZRYYCZMN6EF   NULL   (identifiant WeebCentral)
c4b36f15-4ee5-425f-a77d-…    NULL   (identifiant MangaDex)
100-jours-avant-ta-mort      NULL   (slug SushiScan)
```

Trois formats d'identifiant, trois sources différentes, **aucune source
enregistrée**. C'est précisément l'entrée que `sourceDe()` ne peut pas résoudre,
et `getFrom(undefined, id)` envoie alors la requête à la source courante.

La donnée manquante en base explique le comportement observé côté réseau : la
correction du front (BUG-01) ne suffira pas, il faut aussi **remplir la colonne**
et la rendre obligatoire.

### DB-03 🟠 17 favoris sans titre

```
sushiscan     ellie
weebcentral   01J76XY7E827QQQT0ERKCGH4CD
weebcentral   01J76XYD7E91K8QP6CY0Y53900
```

`favorites` porte `title` et `cover` justement pour éviter de rappeler la
source. Une ligne sans titre force donc un scrape — ou affiche une carte
anonyme.

### DB-04 🟡 `events` n'a pas de colonne `source`

```
id · user_id · type · manga_id · chapter_id · metadata · created_at
```

Le commentaire de `profil.js:1152` le dit déjà. Tant que la colonne n'existe
pas, l'historique ne peut pas savoir de quelle source vient une lecture, et le
repli sur la source courante restera possible.

### DB-05 🟡 `sessions` a un classement différent de toutes les autres tables

```
sessions          utf8mb4_0900_ai_ci
les 22 autres     utf8mb4_unicode_ci
```

Une jointure sur colonne texte entre `sessions` et une autre table lèverait
« Illegal mix of collations ». Aucune requête ne le fait aujourd'hui — c'est un
piège dormant, pas un défaut actif.

### DB-06 🟡 `events` : 4 831 lignes dont 1 390 de plus de 30 jours

Les notifications lues de plus de 30 jours sont effacées (AMEL-53) ; les
événements, non. La table grossit sans fin.

### ✅ Ce qui est solide en base

| Contrôle | Résultat |
|---|---|
| Clés étrangères | **21 déclarées**, toutes `ON DELETE CASCADE` |
| Lignes orphelines (`favorites`, `progress`, `list_items`, `read_chapters`, `events`) | **0** |
| Encodage | **utf8mb4 partout**, aucune colonne en latin1 |
| Moteur | InnoDB sur les 23 tables |
| Emails / pseudos dupliqués | **0** — les contraintes `UNIQUE` tiennent |
| Empreintes de mots de passe non bcrypt | **0** |
| Index | composites bien placés : `favorites(user_id,status)`, `read_chapters(user_id,manga_id)`, `comments(manga_id,created_at DESC)`, `events(user_id,created_at DESC)` |
| `app_settings` | contient bien `local_owner_id` : `resolveOwner()` prend la voie rapide |

---

# PARTIE V — Surface d'API

**67 routes GET appelées une par une**, sans jeton puis avec une session.

| | sans jeton | authentifié |
|---|---|---|
| `200` | 26 | **59** |
| `401` | 37 | 0 |
| `4xx` autres | 2 | 6 |
| `501` | 2 | 2 |
| `429` | 0 | 1 |

**Les 37 `401` sans jeton sont le comportement attendu** — et c'est aussi
l'aveu que mon premier passage ne testait rien de ces routes. Rejoué
authentifié, tout répond.

Anomalies restantes, toutes explicables :
- `501` sur `/chapters/{id}/text` — endpoint texte appelé sur un chapitre
  d'images. Correct.
- `400` sur `/api/img` sans paramètre. Correct.
- `429` sur `/api/me/updates` — **BUG-03**, la seule route qui limite l'app
  elle-même.
- Réponses vides légitimes : `extensions/uninstalled`, `admin/reports`,
  `me/ratings` renvoient `[]`.

### ✅ Confidentialité vérifiée sans authentification

| Requête | Réponse |
|---|---|
| `GET /api/lists/6` (liste **privée**, `is_public=0`) | `404 Liste introuvable` |
| `GET /api/users/profile/Kaito` | `200` mais `private:true`, `hidden:true`, `stats:null` |
| `GET /api/comments/<manga>` | `{"items":[],"total":0}` |
| `GET /api/extensions/health` | `401 Non authentifié` |

Les correctifs SEC-04 et BUG-01 de l'audit de juillet tiennent.

---

# PARTIE VI — Second passage sur les contrôles

Pages jamais auditées, actionnées à leur tour. Détecteur affiné (variation de
classe, de texte ou d'URL au lieu du seul volume de DOM).

### BUG-11 🟠 « Reprendre ma dernière lecture » appelle l'endpoint d'images sur un roman — sur **toutes** les pages

```
501 /api/sources/gutenberg/chapters/2701%3Afull/pages
```
Relevé depuis `parametres.html`, `notifications.html` et `import.html` : le
bouton est dans l'en-tête commun, le défaut est donc global. Même famille que
BUG-01 : la source de la dernière lecture n'est pas résolue.

### BUG-12 🟠 Les cartes de notification déclenchent des scrapes qui expirent

```
504 /api/sources/weebcentral/mangas/:id          via « Nouveau chapitre Shangri-La Fronti… »
504 /api/sources/weebcentral/mangas/:id/…?la…    via « Nouveau chapitre3 Blue Lock · Chap… »
504 /api/sources/weebcentral/…/pages             via « Nouveau chapitre2 Sakamoto Days · … »
```

### BUG-13 🟠 `429 /api/me/updates` sur **12 pages sur 20** — l'app bride l'app

Relevé à l'actionnement des contrôles, sur : `parametres`, `profil`,
`bibliotheque`, `chapitre`, `lecture`, `notifications`, `sources`,
`collections`, `stats`, `recherche`, `import`, `collection-detail`, `index`,
`offline`.

Le déclencheur est toujours le même : un lien de navigation ou le bouton
« Actualiser mes séries ». Ce n'est donc pas un pic accidentel dû à mes sondes,
c'est le chemin normal. Le limiteur ne distingue pas le client officiel d'un
tiers, et `/me/updates` est appelé à chaque changement de page.

Constaté aussi, plus rarement : `429` sur `/api/sources/<src>/mangas/2701`
(depuis `stats.html`) — **le même identifiant Gutenberg envoyé à une autre
source** que dans BUG-01.

### BUG-14 🟡 Deux erreurs isolées, reproduites une fois chacune

```
400 /api/sources/<src>/mangas/search   depuis index.html, via une carte de reprise
502 /api/img                           depuis collection-detail.html, via la recherche
```

### BUG-15 🟡 « Vérifier les mises à jour » (sources) n'affiche aucun retour

Le clic déclenche bien `/api/extensions/updates`, mais le texte de la page ne
bouge pas d'un caractère (4 319 → 4 319) : ni résultat, ni « aucune mise à
jour », ni erreur. L'utilisateur ne peut pas savoir si l'action a eu lieu.

### ERR-05 — Nouveaux faux positifs de mon détecteur, corrigés

| Suspecté inerte | Vérifié |
|---|---|
| Onglets `Toutes` / `Réponses` / `Mentions` (notifications) | **fonctionnent** — l'onglet actif change à chaque clic |
| Bouton `Search` (recherche) | **fonctionne** — champ vide : rien, ce qui est correct ; avec « naruto » : **44 cartes** |
| `Réessayer` (offline) | **fonctionne** — recharge la page ; visité en direct, il n'y a simplement pas de page à retrouver |
| `Aléatoire` (u.html) | même contrôle qu'ailleurs, où il navigue |
| Liens de pied de page, sur les 8 pages où ils réapparaissent | `href` + `target="_blank"` |

Le détecteur est structurellement incapable de voir un changement qui ne touche
ni le volume du DOM, ni les classes racine, ni l'URL. **Toute ligne « inerte »
de ce rapport doit être vérifiée à la main avant d'être traitée comme un
défaut.** C'est la leçon la plus utile de cette campagne.


---

---

# PARTIE VII — Passage approfondi : lecteur, recherche, notifications

Trois zones que le balayage automatique couvrait mal (`lecture.html` :
4 contrôles actionnés sur 52). Reprises à la main, fonction par fonction.

## VII.1 Lecteur de romans — **rien à signaler**

20 contrôles dans la barre d'outils : chapitre précédent/suivant, sommaire
(135 sections), notes de lecture, marquage, **lecture audio (synthèse vocale)**,
téléchargement hors-ligne, réglages, langue, retour en haut.

Le panneau « Réglages de lecture » expose 13 contrôles. Chacun a été actionné et
mesuré sur `.novel-content` :

| Réglage | Effet mesuré |
|---|---|
| Curseur taille (`nsSize`) | 17 px → 13 px |
| Curseur largeur (`nsWidth`) | 720 px → 540 px |
| Police `Sans` | `Bitter` → `Segoe UI` |
| Mode `Pages` | `defilement` → `pages` |

**Persistance** : après rechargement, taille, interligne, largeur, police et
mode sont tous conservés à l'identique.

**Contraste des 4 thèmes du lecteur**, mesuré sur le paragraphe et son fond :

| Thème | Texte | Fond | Contraste |
|---|---|---|---|
| Sombre | `#d8d8de` | `#0d0d0f` | **13,68 : 1** — AAA |
| Noir | `#c9c9cf` | `#000000` | **12,74 : 1** — AAA |
| Sépia | `#3b2f1d` | `#f1e7d0` | **10,61 : 1** — AAA |
| Clair | `#24242a` | `#ffffff` | **15,43 : 1** — AAA |

**Position de lecture** : défilement à 250 000 px, rechargement → restauration à
251 437 px. L'écart de 1 437 px (0,6 %) vient d'un ancrage sur paragraphe, pas
au pixel — c'est le bon comportement pour un texte reflowable.

## VII.2 Barre de recherche de l'en-tête — **rien à signaler**

| Contrôle | Résultat |
|---|---|
| Anti-rebond | **1 seule requête** pour six caractères frappés (`naruto`) |
| Suggestions | boîte affichée, **7 entrées** |
| Validation | `Entrée` → `/recherche.html?q=naruto`, **86 résultats** |
| Chaîne vide / espaces | **ne navigue pas** — reste sur la page |
| Caractères spéciaux | `<script>x</script> & "quote" 100%` → encodé en `%3Cscript%3E…`, **aucune injection** |

**Observation, pas défaut** : les suggestions n'interrogent que la **source
courante** (`/api/sources/weebcentral/mangas/search?q=naruto&limit=6`), alors
que la page de résultats couvre toutes les sources. Un titre absent de la source
courante n'apparaît donc pas en suggestion, mais bien après validation. C'est
défendable — six suggestions ne peuvent pas venir de neuf catalogues — mais
mérite d'être un choix conscient.

## VII.3 Notifications — filtres fonctionnels, erreurs réseau réelles

L'API renvoie **46 notifications en un seul appel** (`?limit=100`) ; le filtrage
est ensuite entièrement côté client.

| Filtre | Contenu mesuré |
|---|---|
| Toutes | 3 825 caractères, entrées « Nouveau chapitre » présentes |
| **Chapitres** | 3 939 caractères, entrées présentes |
| Réponses | 1 093 caractères, aucune entrée |
| Mentions | 979 caractères, aucune entrée |

**BUG-08 de l'audit de juillet (« le filtre Chapitres est toujours vide ») est
corrigé** : le filtre affiche bien les chapitres, et les deux autres se vident
correctement faute de données.

Ce qui reste vrai sur cette page, déjà consigné plus haut : **BUG-12** (les
cartes déclenchent des scrapes qui expirent en 504) et **BUG-13** (`429` sur
`/api/me/updates`).

## VII.4 Ce que ce passage a surtout démontré

Sur **six** anomalies que je croyais tenir dans ces trois zones, **six étaient
des erreurs de mesure de ma part** :

| Ce que j'ai cru | Ce qui était vrai |
|---|---|
| Thèmes Sépia et Clair illisibles (contraste 1,01 : 1) | Je lisais la couleur héritée du **conteneur**, pas celle du contenu. Les 4 thèmes sont **AAA** |
| Position de lecture perdue au rechargement | Test mal construit ; la restauration fonctionne à 0,6 % près |
| `nsLh`, `Serif`, `Mono`, `Noir`, `Clair`, `Défilement` absents | Chaque clic referme le panneau ; je le rouvrais à contretemps |
| Réglages typographiques introuvables | Ils sont dans un panneau, pas dans la barre |
| Notifications vides | Les entrées portent la classe `nt-item`, que mon sélecteur ignorait |
| Police du lecteur à 14 px sans largeur maximale | Je mesurais `main` ; le contenu est à 17 px, largeur 720 px |

Le taux d'erreur de mes propres sondes est le résultat le plus solide de cette
campagne. **Aucun constat de ce rapport ne doit être traité sans avoir relu la
preuve qui l'accompagne** — et une ligne sans preuve mesurée n'est pas un
constat.


---

---

# PARTIE VIII — L'application installée

*Tout ce qui précède a été mesuré sur le serveur de développement. Cette partie
porte sur l'**app desktop 2.5.7 installée**, lancée et interrogée sur son propre
port. C'est un environnement différent : la CSP y est active, le CORS y est
fermé.*

Posture confirmée, distincte du développement :

```
Content-Security-Policy   : présente
CORS depuis un site tiers : refusé (aucun Access-Control-Allow-Origin)
CORS depuis la webview    : autorisé (http://tauri.localhost)
```

## DESK-01 🔴 Les planches d'un chapitre restent **invisibles**

**C'est la panne signalée « les pages des chapitres ne se chargent pas ».**
Elle n'existe **que** dans l'app installée, ce qui explique qu'aucun essai en
développement ne l'ait jamais reproduite.

Mesure sur l'app 2.5.7 installée, chapitre de One Piece :

```
balises .............. 1     l'image est dans le DOM
telechargees ......... 1     complete = true, naturalWidth > 0
classe « loaded » .... 0     jamais posee
opacite .............. 0     ← invisible
visibles ............. 0
violation CSP ........ script-src-attr | inline
```

**L'image se télécharge parfaitement. Elle reste transparente.**

La chaîne complète :

`assets/css/chapitre.css:846-847`
```css
.reader-page-img        { opacity: 0; transition: opacity .25s ease; }
.reader-page-img.loaded { opacity: 1; }
```

`assets/js/chapitre.js:275`
```js
onload="this.classList.add('loaded')" onerror="window.imgFail&&window.imgFail(this)"
```

`server/middleware/security.js:40-54` déclare `scriptSrc: ['unsafe-inline']`
mais **ne déclare pas `scriptSrcAttr`**. Helmet applique alors son défaut
`script-src-attr 'none'` : la CSP autorise le `<script>` en ligne et interdit,
en silence, les gestionnaires en **attribut**. La classe `loaded` n'est donc
jamais posée.

Le commentaire en tête de `security.js` dit d'ailleurs :

> « En local (Laragon) et desktop on tourne en http avec des handlers inline,
> donc elles restent off pour ne rien casser. »

L'intention était juste ; l'audit S-6 a ensuite activé la CSP en desktop sans
que la directive `script-src-attr` suive.

**Vérification du correctif** (essayée puis annulée, le code n'a pas été
modifié) : en déclarant `scriptSrcAttr: ["'unsafe-inline'"]` et en écoutant
`load`/`error` en phase de capture, la même page rend `classe loaded 1`,
`opacite 1`, `visibles 1`, `violations []`.

## DESK-02 🔴 52 gestionnaires en attribut sont morts dans l'app installée

Même cause, portée bien plus large. Recensement exhaustif :

| Fichier | Nb | Ce qui est cassé |
|---|---|---|
| `chapitre.js` | **15** | zoom −/+, plein écran, **zones de changement de page**, vignettes, boutons première/dernière page, révélation des planches, reprise après échec |
| `bibliotheque.js` | 7 | rechargement, couvertures de remplacement (`onerror → this.src=`) |
| `global.js` | 6 | rechargement, masquage des images cassées |
| `accueil.js` | 4 | couvertures de remplacement |
| `catalogue.js` | 4 | **navigation des cartes** (`onclick → location.href=`), couvertures |
| `profil.js` | 4 | **navigation des cartes**, couvertures |
| `recherche.js` | 3 | couvertures de remplacement |
| `serie.js` · `card-hover.js` · `collection-detail.js` · `collections.js` · `downloads-page.js` · `liste.js` · `notes.js` | 1 chacun | couvertures de remplacement |
| `offline.html` | 1 | bouton « Réessayer » |

Deux conséquences distinctes :

1. **Les zones de changement de page du lecteur ne répondent pas.** C'est ce
   que j'avais relevé sans l'expliquer : « la 1re flèche droite reste sans
   effet ». Ce n'était pas un défaut de navigation, c'était la CSP.
2. **Aucune couverture cassée n'est remplacée.** Les 20 `onerror` qui posent une
   image de repli ou masquent l'élément ne s'exécutent jamais : une couverture
   introuvable laisse un trou au lieu du repli prévu.

**Portée de cette découverte sur le reste du rapport** : mon balayage des
786 contrôles a tourné sur le serveur de développement, **où ces 52
gestionnaires fonctionnent**. Le verdict « aucun contrôle mort » vaut donc pour
le site, pas pour l'app installée — où au moins ces 52 le sont.

## DESK-03 🟡 Trois pages sont vides dans l'app

Mesuré sur les 24 pages de l'app installée (longueur du texte rendu) :

| Page | Texte rendu |
|---|---|
| `localreader.html` | **44 caractères** |
| `anilist.html` | **158 caractères** |
| `offline.html` | 271 caractères |

`offline.html` est vide par nature. `localreader.html` (lecture d'un fichier
local) et `anilist.html` n'affichent rien tant qu'aucun fichier n'est déposé et
qu'aucun compte n'est lié — mais rien ne le dit à l'écran : ni consigne, ni
bouton, ni état vide expliqué. Une page qui rend 44 caractères ressemble à une
page cassée.

## DESK-04 — Ce que l'app confirme du reste du rapport

Les défauts relevés en développement se reproduisent à l'identique dans l'app
installée :

```
429 /api/me/updates                    catalogue, bibliotheque      (BUG-13)
504 /api/sources/weebcentral/mangas/…  profil, parametres           (BUG-04)
```

Le journal de démarrage de l'app montre par ailleurs la chaîne complète d'une
source qui expire, ce que le serveur de développement masquait :

```
Error: Source momentanement limitee - reessaie dans un instant
  at friendlyHttpError (…\resources\server\extensions\weebcentral\index.js:48)
  at async Object.track (…\resources\server\lib\source-health.js:53)
  at async chapters (…\resources\server\controllers\manga.controller.js:184)
  { status: 504, upstream: 'weebcentral' }
```

## ✅ Ce qui tient dans l'app installée

| Contrôle | Résultat |
|---|---|
| CSP | **active** — 1 seule violation sur 24 pages, toujours `script-src-attr` |
| CORS site tiers | **refusé** |
| CORS webview | autorisé (correctif 2.5.4 en place) |
| Erreurs JS hors CSP | **aucune** sur les 24 pages |
| Rendu | 21 pages sur 24 affichent leur contenu |


---

# PARTIE IX — Ce qui n'a pas été audité

- **Les actions destructives** : supprimer, vider, réinitialiser, déconnecter,
  importer, exporter, restaurer. Recensées, jamais déclenchées. À faire sur une
  base jetable.
- **585 contrôles distincts sur 1 371** n'ont pas été actionnés. Les pages
  terminées : `notifications` 82/89, `import` 26/31, `parametres` 72/80,
  `anilist` 2/2, `localreader` 1/1. Les inachevées : `bibliotheque` 189/436,
  `lecture` 4/52, `recherche` 24/74, `index` 53/101, `sources` 40/59, `u` 23/41.
  Les pages `serie`, `chapitre` et `collection-detail` n'ont été vues qu'avec
  **un seul** identifiant.
- **Les routes d'écriture** (61 sur 128) : seules les 67 routes GET ont été
  appelées. POST, PUT et DELETE modifient les données — même raison que pour les
  actions destructives.
- **Parcours multi-comptes** : commentaires, notes, listes partagées, profils
  publics. Tout a été vu sous le compte propriétaire.
- **L'app desktop, contrôle par contrôle** : ses 24 pages ont été balayées
  (partie VIII), mais les 786 contrôles n'ont été actionnés que sur le serveur
  de développement. Or DESK-02 montre que 52 d'entre eux sont morts dans l'app.
  Le balayage des contrôles est donc **à refaire sur l'app installée**.
- **Montée en charge** : rien au-delà d'une bibliothèque personnelle.
- **Autres navigateurs que Chromium**, et lecteurs d'écran réels — axe-core ne
  détecte que 30 à 50 % des défauts d'accessibilité.
