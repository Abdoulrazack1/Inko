# Build & deps
**/node_modules
desktop/dist
desktop/node_modules
server/node_modules

# Git & éditeurs
.git
.github
.vscode
**/.DS_Store

# Secrets & config locale (injectés via variables d'environnement)
server/.env
server/config/*.json

# Artefacts mobiles / divers
android
promo
tools
*.log

# Captures d'écran du README : 5,7 Mo qu'aucune page ne charge.
# `COPY assets/ /app/frontend/assets/` prend le dossier entier, et ces images
# ne servent qu'à la page GitHub du dépôt — elles doivent rester versionnées,
# mais n'ont rien à faire dans l'image ni sur le serveur.
assets/screenshots
