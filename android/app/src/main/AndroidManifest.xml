<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        
            <!-- Sans cette meta-data, le fichier shortcuts.xml est ignore : un
                 appui long sur l'icone ne montre rien, et rien ne le signale.
                 Elle doit etre dans l'activite qui porte LAUNCHER. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- `navigator.vibrate()` est SANS EFFET dans un WebView tant que
         l'application ne déclare pas cette permission — sans erreur, sans
         exception : l'appel réussit et rien ne se passe. L'appui long sur une
         carte perdrait alors son seul retour immédiat, puisque rien à l'écran
         ne change avant que la feuille monte. Permission normale : elle ne
         provoque aucune demande à l'utilisateur. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- P2.8 — découverte du hub par mDNS (`NsdManager`).
         Sans CHANGE_WIFI_MULTICAST_STATE, le Wi-Fi filtre les paquets
         multicast avant même qu'ils atteignent l'application : la découverte
         ne trouve RIEN, sans aucune erreur, et l'échec ressemble à un hub
         absent du réseau. C'est une permission « normale » : elle n'entraîne
         aucune demande à l'utilisateur.
         ACCESS_NETWORK_STATE sert au greffon Network, qui détecte le
         changement de Wi-Fi (maison → travail). -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>
